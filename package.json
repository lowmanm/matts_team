{
  "name": "matts_team",
  "version": "0.1.0",
  "description": "GSD workflow template for VS Code — build applications locally with Claude via Copilot",
  "type": "module",
  "scripts": {
    "build": "tsc",
    "typecheck": "tsc --noEmit",
    "sync": "gsd sync"
  },
  "devDependencies": {
    "@matts-team/gsd": "^1.0.0",
    "typescript": "^5.4.0"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
